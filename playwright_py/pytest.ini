[pytest]
addopts = -q -rA --maxfail=1 --html=reports/report.html --self-contained-html
testpaths = tests
markers = 
    e2e: end-to-end scenarios
    smoke: quick health checks
    flaky: temporarily unstable tests
filterwarnings =
    ignore:DeprecationWarning
    ignore:UserWarning